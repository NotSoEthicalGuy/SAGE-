'use client';

import PlaceholderPage from '@/components/PlaceholderPage';

export default function AdminSettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Configure system preferences, access policies, and global defaults."
    />
  );
}
